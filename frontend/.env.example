# Client Environment Variables

# Lokalnie NIE jest potrzebne (proxy w vite.config.ts obsługuje /api → localhost:3001)
# Przy deployu na Vercel ustaw tę zmienną w panelu: Settings → Environment Variables
# VITE_API_URL=http://localhost:3001
VITE_API_URL=https://TWOJ-BACKEND.railway.app
